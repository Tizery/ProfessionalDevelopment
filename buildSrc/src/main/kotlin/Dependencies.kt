object Dependencies {
    private const val CORE_KTX_VERSION = "1.7.0"
    const val CORE_KTX = "androidx.core:core-ktx:${CORE_KTX_VERSION}"

    private const val APP_COMPAT_VERSION = "1.4.1"
    const val APP_COMPAT = "androidx.appcompat:appcompat:${APP_COMPAT_VERSION}"

    private const val MATERIAL_VERSION = "1.5.0"
    const val MATERIAL = "com.google.android.material:material:${MATERIAL_VERSION}"

    private const val CONSTRAINT_LAYOUT_VERSION = "2.1.3"
    const val CONSTRAINT_LAYOUT =
        "androidx.constraintlayout:constraintlayout:${CONSTRAINT_LAYOUT_VERSION}"

    private const val SWIPE_REFRESH_LAYOUT_VERSION="1.1.0"
    const val SWIPE_REFRESH_LAYOUT="androidx.swiperefreshlayout:swiperefreshlayout:${SWIPE_REFRESH_LAYOUT_VERSION}"

    //Retrofit2
    private const val RETROFIT_VERSION = "2.6.0"
    const val RETROFIT = "com.squareup.retrofit2:retrofit:${RETROFIT_VERSION}"

    private const val CONVERTER_GSON_VERSION = "2.6.0"
    const val CONVERTER_GSON = "com.squareup.retrofit2:converter-gson:${CONVERTER_GSON_VERSION}"

    private const val OKHTTP3_LOGGING_INTERCEPTOR_VERSION = "3.12.1"
    const val OKHTTP3_LOGGING_INTERCEPTOR =
        "com.squareup.okhttp3:logging-interceptor:${OKHTTP3_LOGGING_INTERCEPTOR_VERSION}"

    private const val RETROFIT2_KOTLIN_COROUTINES_ADAPTER_VERSION = "0.9.2"
    const val RETROFIT2_KOTLIN_COROUTINES_ADAPTER =
        "com.jakewharton.retrofit:retrofit2-kotlin-coroutines-adapter:${RETROFIT2_KOTLIN_COROUTINES_ADAPTER_VERSION}"

    //RxJava
    private const val RX_ANDROID_VERSION = "2.1.0"
    const val RX_ANDROID = "io.reactivex.rxjava2:rxandroid:${RX_ANDROID_VERSION}"

    private const val RX_JAVA_VERSION = "2.2.8"
    const val RX_JAVA = "io.reactivex.rxjava2:rxjava:${RX_JAVA_VERSION}"

    //Koin
    private const val KOIN_CORE_VERSION = "3.1.2"
    const val KOIN_CORE = "io.insert-koin:koin-core:${KOIN_CORE_VERSION}"

    private const val KOIN_ANDROID_VERSION = "3.1.2"
    const val KOIN_ANDROID = "io.insert-koin:koin-android:${KOIN_ANDROID_VERSION}"

    private const val KOIN_ANDROID_COMPAT_VERSION = "3.1.2"
    const val KOIN_ANDROID_COMPAT =
        "io.insert-koin:koin-android-compat:${KOIN_ANDROID_COMPAT_VERSION}"

    //Picasso
    private const val PICASSO_VERSION = "2.71828"
    const val PICASSO = "com.squareup.picasso:picasso:${PICASSO_VERSION}"

    //Glide
    private const val GLIDE_VERSION = "4.11.0"
    const val GLIDE = "com.github.bumptech.glide:glide:${GLIDE_VERSION}"

    private const val GLIDE_COMPILER_VERSION = "4.11.0"
    const val GLIDE_COMPILER = "com.github.bumptech.glide:compiler:${GLIDE_COMPILER_VERSION}"

    //Coil
    private const val COIL_VERSION = "0.11.0"
    const val COIL = "io.coil-kt:coil:${COIL_VERSION}"

    //Room
    private const val ROOM_RUNTIME_VERSION = "2.4.2"
    const val ROOM_RUNTIME = "androidx.room:room-runtime:${ROOM_RUNTIME_VERSION}"

    private const val ROOM_COMPILER_VERSION = "2.4.2"
    const val ROOM_COMPILER = "androidx.room:room-compiler:${ROOM_COMPILER_VERSION}"

    private const val ROOM_KTX_VERSION = "2.4.2"
    const val ROOM_KTX = "androidx.room:room-ktx:${ROOM_KTX_VERSION}"

    //Test
    private const val JUNIT_VERSION = "4.+"
    const val JUNIT = "junit:junit:${JUNIT_VERSION}"

    private const val TEST_EXT_JUNIT_VERSION = "1.1.3"
    const val TEST_EXT_JUNIT = "androidx.test.ext:junit:${TEST_EXT_JUNIT_VERSION}"

    private const val TEST_ESPRESSO_CORE_VERSION = "3.4.0"
    const val TEST_ESPRESSO_CORE =
        "androidx.test.espresso:espresso-core:${TEST_ESPRESSO_CORE_VERSION}"
}
